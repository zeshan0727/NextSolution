name: Build Next Reminder v1.3.12

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout pull request
        uses: actions/checkout@v4

      - name: Build and verify v1.3.12
        run: |
          chmod +x NextReminderApp/ci/build-v142.sh
          set -o pipefail
          NextReminderApp/ci/build-v142.sh 2>&1 | tee v142-build.log

      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: NextReminder-v1.3.12
          if-no-files-found: warn
          path: |
            NextReminderApp/NextReminder-v1.3.12-unsigned.tipa
            NextReminderApp/NextReminder-v1.3.12-unsigned.ipa
            NextReminderApp/NextReminder-v1.3.12-unsigned.tipa.sha256
            NextReminderApp/xcodebuild.log
            v142-build.log
