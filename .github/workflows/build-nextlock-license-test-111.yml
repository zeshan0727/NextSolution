name: NextLock 1.1.2 License Test (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired_test_notice:
    runs-on: ubuntu-latest
    steps:
      - name: License test retired
        run: echo "NextLock 1.1.2 is moving to the live release workflow. This old license-test publisher is intentionally manual-only."
