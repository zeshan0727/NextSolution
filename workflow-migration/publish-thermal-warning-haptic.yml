name: Build Validate and Publish Thermal Warning Haptic

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/publish-thermal-warning-haptic.yml"
      - "DailyTweaks/2026-08-08-ThermalWarningHaptic/**"
      - "ReleaseTriggers/thermal-warning-haptic.txt"
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/publish-thermal-warning-haptic.yml"
      - "DailyTweaks/2026-08-08-ThermalWarningHaptic/**"
      - "ReleaseTriggers/thermal-warning-haptic.txt"

permissions:
  contents: write

concurrency:
  group: publish-thermal-warning-haptic-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-validate-publish:
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}

      - name: Install packaging tools
        run: brew install dpkg ldid xz zstd

      - name: Validate source and runtime guards
        shell: bash
        run: |
          set -euo pipefail
          P="$GITHUB_WORKSPACE/DailyTweaks/2026-08-08-ThermalWarningHaptic"
          python3 "$P/tests/test_runtime_logic.py"
          grep -q '^Package: com.nextsolution.thermalwarninghaptic$' "$P/control"
          grep -q '^Version: 1.0.0$' "$P/control"
          grep -q '^Architecture: iphoneos-arm64$' "$P/control"
          grep -q '^Homepage: https://nextjailbreak.com/$' "$P/control"
          grep -q '^Depiction: https://nextjailbreak.com/depictions/thermalwarninghaptic.html$' "$P/control"
          ! grep -R -q 'zeshan0727.github.io' "$P" depictions/thermalwarninghaptic.html
          grep -q '^ARCHS = arm64 arm64e$' "$P/Makefile"
          grep -q '^TARGET = iphone:clang:latest:15.0$' "$P/Makefile"
          grep -q '^THEOS_PACKAGE_SCHEME ?= roothide$' "$P/Makefile"
          grep -q 'segalign,4000' "$P/Makefile"
          grep -q 'com.apple.springboard' "$P/ThermalWarningHaptic.plist"
          grep -q 'NSProcessInfoThermalStateDidChangeNotification' "$P/Tweak.xm"
          grep -q 'TWHObserver =' "$P/Tweak.xm"
          grep -q 'UINotificationFeedbackTypeWarning' "$P/Tweak.xm"
          grep -q 'notificationOccurred:UINotificationFeedbackTypeWarning' "$P/Tweak.xm"
          python3 - "$P/Tweak.xm" <<'PY'
          from pathlib import Path
          import sys
          t=Path(sys.argv[1]).read_text()
          ctor=t.index('%ctor')
          tail=t[ctor:]
          read=tail.index('TWHLastState = processInfo.thermalState')
          observe=tail.index('addObserverForName:NSProcessInfoThermalStateDidChangeNotification')
          assert read < observe, 'thermalState must be read before observer registration'
          assert 'static id TWHObserver = nil;' in t
          assert 'TWHHasInitialState' in t
          assert '!TWHIsHotState(previousState) &&' in t and 'TWHIsHotState(currentState)' in t
          PY
          grep -q 'ThermalWarningHapticPreferences_LDFLAGS = -Wl,-undefined,dynamic_lookup -Wl,-segalign,4000' "$P/Preferences/Makefile"
          ! grep -q 'PRIVATE_FRAMEWORKS = Preferences' "$P/Preferences/Makefile"
          grep -q 'ThermalWarningHapticPreferences_RESOURCE_DIRS = Resources' "$P/Preferences/Makefile"
          grep -q 'CFPreferencesCopyAppValue' "$P/Preferences/TWHRootListController.m"
          grep -q 'CFPreferencesSetAppValue' "$P/Preferences/TWHRootListController.m"
          grep -q 'CFNotificationCenterPostNotification' "$P/Preferences/TWHRootListController.m"
          grep -q 'Enable Thermal Alert' "$P/Preferences/Resources/Root.plist"
          test "$(grep -c 'PSSwitchCell' "$P/Preferences/Resources/Root.plist")" -eq 1
          ! grep -Eq 'Test|Diagnostic|PID|Hook|Runtime|Class|Selector' "$P/Preferences/Resources/Root.plist"
          plutil -lint "$P/Preferences/Resources/Root.plist"
          plutil -lint "$P/Preferences/Resources/Info.plist"
          plutil -lint "$P/layout/Library/PreferenceLoader/Preferences/ThermalWarningHaptic.plist"

      - name: Build rootless
        shell: bash
        run: |
          set -euo pipefail
          P="$GITHUB_WORKSPACE/DailyTweaks/2026-08-08-ThermalWarningHaptic"; O="$RUNNER_TEMP/twh-release"; L="$RUNNER_TEMP/twh-logs"
          mkdir -p "$O" "$L"
          git clone --recursive --depth 1 https://github.com/theos/theos.git "$RUNNER_TEMP/theos-rootless" > "$L/rootless-bootstrap.log" 2>&1
          export THEOS="$RUNNER_TEMP/theos-rootless" THEOS_PACKAGE_SCHEME=rootless
          rm -rf "$P/packages"
          make -C "$P" clean package FINALPACKAGE=1 2>&1 | tee "$L/rootless-build.log"
          D="$(find "$P/packages" -type f -name '*_iphoneos-arm64.deb' -print -quit)"; test -n "$D"
          cp "$D" "$O/ThermalWarningHaptic_1.0.0_Rootless.deb"

      - name: Build RootHide
        shell: bash
        run: |
          set -euo pipefail
          P="$GITHUB_WORKSPACE/DailyTweaks/2026-08-08-ThermalWarningHaptic"; O="$RUNNER_TEMP/twh-release"; L="$RUNNER_TEMP/twh-logs"
          git checkout -- "$P/control"
          git init "$RUNNER_TEMP/theos-roothide" > "$L/roothide-bootstrap.log" 2>&1
          git -C "$RUNNER_TEMP/theos-roothide" remote add origin https://github.com/roothide/theos.git >> "$L/roothide-bootstrap.log" 2>&1
          git -C "$RUNNER_TEMP/theos-roothide" fetch --depth 1 origin 88506b2c22e9e07dd4ed055f23c9e398a117a2c7 >> "$L/roothide-bootstrap.log" 2>&1
          git -C "$RUNNER_TEMP/theos-roothide" checkout --detach 88506b2c22e9e07dd4ed055f23c9e398a117a2c7 >> "$L/roothide-bootstrap.log" 2>&1
          git -C "$RUNNER_TEMP/theos-roothide" submodule update --init --recursive --depth 1 >> "$L/roothide-bootstrap.log" 2>&1
          sed -i '' 's/^Architecture: iphoneos-arm64$/Architecture: iphoneos-arm64e/' "$P/control"
          export THEOS="$RUNNER_TEMP/theos-roothide" THEOS_PACKAGE_SCHEME=roothide
          rm -rf "$P/packages"
          make -C "$P" clean package FINALPACKAGE=1 2>&1 | tee "$L/roothide-build.log"
          D="$(find "$P/packages" -type f -name '*_iphoneos-arm64e.deb' -print -quit)"; test -n "$D"
          cp "$D" "$O/ThermalWarningHaptic_1.0.0_RootHide.deb"
          git checkout -- "$P/control"

      - name: Upload compiler logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: thermal-warning-haptic-compiler-logs-${{ github.run_id }}
          path: ${{ runner.temp }}/twh-logs/
          if-no-files-found: warn

      - name: Validate packages
        shell: bash
        run: |
          set -euo pipefail
          O="$RUNNER_TEMP/twh-release"
          check() {
            D="$1"; A="$2"; X="$(mktemp -d)"
            test "$(dpkg-deb -f "$D" Package)" = com.nextsolution.thermalwarninghaptic
            test "$(dpkg-deb -f "$D" Name)" = 'Thermal Warning Haptic'
            test "$(dpkg-deb -f "$D" Version)" = 1.0.0
            test "$(dpkg-deb -f "$D" Architecture)" = "$A"
            dpkg-deb -f "$D" Depends | grep -q 'firmware (>= 15.0)'
            dpkg-deb -f "$D" Depends | grep -q mobilesubstrate
            dpkg-deb -f "$D" Depends | grep -q preferenceloader
            dpkg-deb -x "$D" "$X"
            F="$(find "$X" -type f -path '*MobileSubstrate/DynamicLibraries/ThermalWarningHaptic.plist' -print -quit)"
            R="$(find "$X" -type f -path '*ThermalWarningHapticPreferences.bundle/Root.plist' -print -quit)"
            E="$(find "$X" -type f -path '*PreferenceLoader/Preferences/ThermalWarningHaptic.plist' -print -quit)"
            Y="$(find "$X" -type f -name ThermalWarningHaptic.dylib -print -quit)"
            test -n "$F"; test -n "$R"; test -n "$E"; test -s "$Y"
            grep -q 'com.apple.springboard' "$F"
            grep -q 'Enable Thermal Alert' "$R"
            test "$(grep -c 'PSSwitchCell' "$R")" -eq 1
            grep -a -q NSProcessInfoThermalStateDidChangeNotification "$Y"
            grep -a -q UINotificationFeedbackGenerator "$Y"
            grep -a -q 'notificationOccurred:' "$Y"
            rm -rf "$X"
          }
          check "$O/ThermalWarningHaptic_1.0.0_Rootless.deb" iphoneos-arm64
          check "$O/ThermalWarningHaptic_1.0.0_RootHide.deb" iphoneos-arm64e
          {
            echo 'Thermal Warning Haptic 1.0.0: BUILD VALIDATION PASS'
            echo 'Deterministic runtime checks: PASS (8 cases)'
            echo 'Rootless package: PASS'
            echo 'RootHide package: PASS'
            shasum -a 256 "$O"/*.deb
          } | tee "$RUNNER_TEMP/twh-validation.txt"

      - name: Upload validation report
        uses: actions/upload-artifact@v4
        with:
          name: thermal-warning-haptic-validation-${{ github.run_id }}
          path: ${{ runner.temp }}/twh-validation.txt

      - name: Publish Sileo packages and indexes
        if: github.event_name == 'push'
        shell: bash
        run: |
          set -euo pipefail
          O="$RUNNER_TEMP/twh-release"; S="$RUNNER_TEMP/twh-scan"
          rm -rf "$S"; mkdir -p "$S"; cp "$O"/*.deb "$S/"
          dpkg-scanpackages -m "$S" /dev/null > "$RUNNER_TEMP/twh-stanzas"
          sed -i '' 's|^Filename: .*/twh-scan/|Filename: ./debfiles/|' "$RUNNER_TEMP/twh-stanzas"
          git config user.name 'Next Jailbreak Build Bot'
          git config user.email 'actions@users.noreply.github.com'
          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            mkdir -p debfiles; cp "$O"/*.deb debfiles/
            python3 - "$RUNNER_TEMP/twh-stanzas" <<'PY'
          from pathlib import Path
          import re,sys
          p=Path('Packages'); old=p.read_text() if p.exists() else ''
          rows=[x.strip() for x in re.split(r'\n\s*\n',old) if x.strip()]
          rows=[x for x in rows if not re.search(r'^Package: com\.nextsolution\.thermalwarninghaptic$',x,re.M)]
          new=[x.strip() for x in re.split(r'\n\s*\n',Path(sys.argv[1]).read_text()) if x.strip()]
          p.write_text('\n\n'.join(rows+new)+'\n')
          PY
            test "$(grep -c '^Package: com.nextsolution.thermalwarninghaptic$' Packages)" -eq 2
            test "$(grep -c '^Filename: ./debfiles/ThermalWarningHaptic_1.0.0_' Packages)" -eq 2
            gzip -9 -c Packages > Packages.gz
            bzip2 -9 -c Packages > Packages.bz2
            xz -9e -c Packages > Packages.xz
            zstd -19 -q -c Packages > Packages.zst
            python3 - <<'PY'
          from datetime import datetime,timezone
          from pathlib import Path
          import re
          p=Path('Release'); t=p.read_text()
          m=re.search(r'^Version: (\d+)\.(\d+)\.(\d+)$',t,re.M)
          if m: t=re.sub(r'^Version: .*$',f'Version: {m.group(1)}.{m.group(2)}.{int(m.group(3))+1}',t,flags=re.M)
          t=re.sub(r'^Date: .*$', 'Date: '+datetime.now(timezone.utc).strftime('%a, %d %b %Y %H:%M:%S UTC'),t,flags=re.M)
          t=re.sub(r'^Icon: .*$', 'Icon: https://nextjailbreak.com/CydiaIcon.png',t,flags=re.M)
          p.write_text(t)
          PY
            git add debfiles/ThermalWarningHaptic_1.0.0_Rootless.deb debfiles/ThermalWarningHaptic_1.0.0_RootHide.deb Packages Packages.gz Packages.bz2 Packages.xz Packages.zst Release
            git diff --cached --check
            git commit -m 'Publish Thermal Warning Haptic 1.0.0'
            git push origin HEAD:main && exit 0
            sleep $((attempt*4))
          done
          exit 1
